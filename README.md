# QU Tools

The supported internal tool is in [`plu_matcher/`](plu_matcher/README.md).

Start there:

```powershell
cd plu_matcher
.\setup.ps1
.\run.ps1 --help
```

Real credentials, cached QU responses, company workbooks, and generated run
artifacts are intentionally excluded from Git.
